From 4badb5c12346394395ae8779f76c6a4f41fb010c Mon Sep 17 00:00:00 2001
From: liangliang678 <wujunliang@loongson.cn>
Date: Thu, 6 Jul 2023 16:14:23 +0800
Subject: [PATCH] exp14

---
 exe_stage.v |  54 +++++++++++++++------------
 id_stage.v  |  18 +++++----
 if_stage.v  | 104 +++++++++++++++++++++++++++++++++++++++++++---------
 mem_stage.v |  73 +++++++++++++++++++++++-------------
 mycpu.h     |   4 +-
 mycpu_top.v |  44 +++++++++++++++-------
 6 files changed, 207 insertions(+), 90 deletions(-)

diff --git a/exe_stage.v b/exe_stage.v
index 21016c6..cdb19f5 100644
--- a/exe_stage.v
+++ b/exe_stage.v
@@ -26,10 +26,13 @@ module exe_stage(
     input  wire        refetch_flush ,
     input  wire        ms_excp       ,
     // data sram interface
-    output wire        data_sram_en   ,
-    output wire [ 3:0] data_sram_we   ,
-    output wire [31:0] data_sram_addr ,
-    output wire [31:0] data_sram_wdata
+    output wire        data_req    ,
+    output wire        data_wr     ,
+    output wire [ 1:0] data_size   ,
+    output wire [31:0] data_addr   ,
+    output wire [ 3:0] data_wstrb  ,
+    output wire [31:0] data_wdata  ,
+    input  wire        data_addr_ok
 );
 
 reg         es_valid      ;
@@ -99,16 +102,17 @@ wire excp;
 wire [ 5:0] excp_num;
 wire excp_ale;
 
-assign es_to_ms_bus = {excp_num       ,  //164:159
-                       es_ertn        ,  //158:158
-                       excp           ,  //157:157
-                       es_csr_we      ,  //156:156
-                       es_csr_addr    ,  //155:142
-                       csr_final_wdata,  //141:110
-                       es_mem_size    ,  //109:108
-                       es_mem_sign_ext,  //107:107
-                       es_alu_result  ,  //106:75
-                       es_mul_div_op  ,  //74:71
+assign es_to_ms_bus = {excp_num       ,  //165:160
+                       es_ertn        ,  //159:159
+                       excp           ,  //158:158
+                       es_csr_we      ,  //157:157
+                       es_csr_addr    ,  //156:143
+                       csr_final_wdata,  //142:111
+                       es_mem_size    ,  //110:109
+                       es_mem_sign_ext,  //108:108
+                       es_alu_result  ,  //107:76
+                       es_mul_div_op  ,  //75:72
+                       es_mem_we      ,  //71:71
                        es_res_from_mem,  //70:70
                        es_gr_we       ,  //69:69
                        es_dest        ,  //68:64
@@ -116,7 +120,9 @@ assign es_to_ms_bus = {excp_num       ,  //164:159
                        es_pc             //31:0
                       };
 
-assign es_ready_go    = !div_stall || excp;
+assign es_ready_go    = data_req && data_addr_ok || 
+                        !es_mem_we && !es_res_from_mem && !div_stall || 
+                        excp;
 assign es_allowin     = !es_valid || es_ready_go && ms_allowin;
 assign es_to_ms_valid =  es_valid && es_ready_go;
 always @(posedge clk) begin
@@ -176,15 +182,15 @@ wire [31:0] es_stb_data = {{8{es_stb_wen[3]}} & es_rkd_value[7:0] ,
 wire [31:0] es_sth_data = {{16{es_sth_wen[3]}} & es_rkd_value[15:0] ,
                            {16{es_sth_wen[0]}} & es_rkd_value[15:0]};
 
-assign data_sram_en    = (es_res_from_mem || es_mem_we) && es_valid && !flush_sign && !ms_excp && !excp;
-assign data_sram_we    = {4{es_mem_we && !flush_sign && !ms_excp && !excp && es_mem_size[0]}} & es_stb_wen |
-                         {4{es_mem_we && !flush_sign && !ms_excp && !excp && es_mem_size[1]}} & es_sth_wen |
-                         {4{es_mem_we && !flush_sign && !ms_excp && !excp && !es_mem_size  }} & 4'hf       |
-                         {4{!es_mem_we || flush_sign || ms_excp || excp                    }} & 4'h0;
-assign data_sram_addr  = es_alu_result;
-assign data_sram_wdata = ({32{es_mem_size[0]}} & es_stb_data ) |
-                         ({32{es_mem_size[1]}} & es_sth_data ) |
-                         ({32{!es_mem_size  }} & es_rkd_value) ;
+assign data_req    = (es_res_from_mem || es_mem_we) && es_valid && ms_allowin && !flush_sign && !ms_excp && !excp;
+assign data_wr     = es_mem_we;
+assign {data_wstrb, data_size}  = ({6{es_mem_size[0]}} & {es_stb_wen, 2'b00}) |
+                                  ({6{es_mem_size[1]}} & {es_sth_wen, 2'b01}) |
+                                  ({6{!es_mem_size  }} & {4'b1111   , 2'b10}) ;
+assign data_addr  = es_alu_result;
+assign data_wdata = ({32{es_mem_size[0]}} & es_stb_data ) |
+                    ({32{es_mem_size[1]}} & es_sth_data ) |
+                    ({32{!es_mem_size  }} & es_rkd_value) ;
 
 // csr
 assign csr_mask_wdata  = (es_rj_value & es_rkd_value) | (~es_rj_value & es_csr_data);
diff --git a/id_stage.v b/id_stage.v
index 2080d1c..4fe0a9f 100644
--- a/id_stage.v
+++ b/id_stage.v
@@ -195,10 +195,12 @@ assign {es_dep_need_stall,
         es_forward_data
        } = es_to_ds_forward_bus;
 
+wire        ms_dep_need_stall;
 wire        ms_forward_enable;
 wire [ 4:0] ms_forward_reg;
 wire [31:0] ms_forward_data;
-assign {ms_forward_enable,
+assign {ms_dep_need_stall,
+        ms_forward_enable,
         ms_forward_reg,
         ms_forward_data
        } = ms_to_ds_forward_bus;
@@ -215,13 +217,13 @@ assign {ws_forward_enable,
 wire        rj_eq_rd;
 wire        rj_lt_rd_unsign;
 wire        rj_lt_rd_sign;
-wire        br_taken_cancel;
+wire        br_stall;
 wire        br_taken;
 
 wire [31:0] br_offs;
 wire [31:0] jirl_offs;
 wire [31:0] br_target;
-assign br_bus       = {br_taken_cancel,br_taken,br_target};
+assign br_bus       = {br_stall,br_taken,br_target};
 
 // excp
 wire excp;
@@ -263,7 +265,7 @@ always @(posedge clk) begin
     else if (flush_sign) begin
         ds_valid <= 1'b0;
     end
-    else if (br_taken_cancel) begin
+    else if (br_taken && ds_allowin) begin
         ds_valid <= 1'b0;
     end
     else if (ds_allowin) begin 
@@ -641,9 +643,11 @@ assign inst_need_rkd =  inst_add_w  |
                         inst_csrxchg;
 
 assign rf1_stall = inst_need_rj &&
-                   (es_dep_need_stall && (es_forward_reg == rf_raddr1));
+                   ((es_dep_need_stall && (es_forward_reg == rf_raddr1)) ||
+                    (ms_dep_need_stall && (ms_forward_reg == rf_raddr1)));
 assign rf2_stall = inst_need_rkd &&
-                   (es_dep_need_stall && (es_forward_reg == rf_raddr2));
+                   ((es_dep_need_stall && (es_forward_reg == rf_raddr2)) ||
+                    (ms_dep_need_stall && (ms_forward_reg == rf_raddr2)));
 
 assign rj_value  = es_forward_enable && (es_forward_reg == rf_raddr1) ? es_forward_data :
                    ms_forward_enable && (ms_forward_reg == rf_raddr1) ? ms_forward_data :
@@ -660,7 +664,7 @@ assign rj_lt_rd_unsign = (rj_value < rkd_value);
 assign rj_lt_rd_sign   = (rj_value[31] && ~rkd_value[31]) ? 1'b1 :
                          (~rj_value[31] && rkd_value[31]) ? 1'b0 : rj_lt_rd_unsign;                         
 
-assign br_taken_cancel = br_taken && ds_allowin;
+assign br_stall = br_taken && (rf1_stall || rf2_stall);
 assign br_taken = (   inst_beq  &&  rj_eq_rd
                    || inst_bne  && !rj_eq_rd
                    || inst_blt  &&  rj_lt_rd_sign
diff --git a/if_stage.v b/if_stage.v
index af358b4..8c5a8e3 100644
--- a/if_stage.v
+++ b/if_stage.v
@@ -18,28 +18,35 @@ module if_stage(
     input  wire [31:0]                  csr_eentry       ,
     input  wire [31:0]                  csr_era          ,
     // inst sram interface
-    output wire        inst_sram_en   ,
-    output wire [ 3:0] inst_sram_wen  ,
-    output wire [31:0] inst_sram_addr ,
-    output wire [31:0] inst_sram_wdata,
-    input  wire [31:0] inst_sram_rdata
+    output wire        inst_req    ,
+    output wire        inst_wr     ,
+    output wire [ 1:0] inst_size   ,
+    output wire [31:0] inst_addr   ,
+    output wire [ 3:0] inst_wstrb  ,
+    output wire [31:0] inst_wdata  ,
+    input  wire        inst_addr_ok,
+    input  wire        inst_data_ok,
+    input  wire [31:0] inst_rdata  
 );
 
 reg         fs_valid;
 wire        fs_ready_go;
 wire        fs_allowin;
 wire        to_fs_valid;
+wire        pfs_readg_go;
 wire        pfs_excp;
 wire [ 0:0] pfs_excp_num;
 
 wire flush_sign = excp_flush || ertn_flush || refetch_flush;
 
-wire         br_taken_cancel;
+wire         br_stall;
 wire         br_taken;
 wire [ 31:0] br_target;
-assign {br_taken_cancel, br_taken, br_target} = br_bus;
+assign {br_stall, br_taken, br_target} = br_bus;
 
 wire [31:0] fs_inst;
+reg         fs_inst_valid;
+reg  [31:0] fs_inst_r;
 reg  [31:0] fs_pc;
 reg         fs_excp;
 reg  [ 0:0] fs_excp_num;
@@ -49,23 +56,77 @@ assign fs_to_ds_bus = {fs_excp    ,
                        fs_pc      };
 
 // pre-IF stage
+reg                  flush_valid;
+reg [31:0] flush_target;
+
+always @(posedge clk) begin
+    if (reset) begin
+        flush_valid <= 1'b0;
+    end
+    else if (inst_req && inst_addr_ok) begin
+        flush_valid <= 1'b0;
+    end
+    else if (!(inst_req && inst_addr_ok)) begin
+        if (excp_flush) begin
+            flush_valid <= 1'b1;
+            flush_target  <= csr_eentry;
+        end
+        else if (ertn_flush) begin
+            flush_valid <= 1'b1;
+            flush_target  <= csr_era;
+        end
+        else if (refetch_flush) begin
+            flush_valid <= 1'b1;
+            flush_target  <= ws_pc + 3'h4;
+        end
+        else if (br_taken && !br_stall) begin
+            flush_valid <= 1'b1;
+            flush_target  <= br_target;
+        end
+    end 
+end
+
 wire [31:0] seq_pc;
 wire [31:0] nextpc;
-assign to_fs_valid  = ~reset;
+assign to_fs_valid  = ~reset && pfs_readg_go;
+assign pfs_readg_go = inst_req && inst_addr_ok || pfs_excp;
 assign seq_pc       = fs_pc + 3'h4;
 assign nextpc       = excp_flush                   ? csr_eentry :
                       ertn_flush                   ? csr_era    :
                       refetch_flush                ? ws_pc + 3'h4 :
-                      br_taken                     ? br_target  : 
+                      flush_valid                  ? flush_target :
+                      br_taken && !br_stall        ? br_target  : 
                                                      seq_pc; 
 
+reg wait_data_ok;
+
+assign inst_req    = fs_allowin && !wait_data_ok && !br_stall && !pfs_excp;
+assign inst_wr     = 1'h0;
+assign inst_size   = 2'h2;
+assign inst_addr   = nextpc;
+assign inst_wstrb  = 4'b0;
+assign inst_wdata  = 32'b0;
+
 wire pfs_excp_adef = (nextpc[0] || nextpc[1]);
 
 assign pfs_excp = pfs_excp_adef;
 assign pfs_excp_num = {pfs_excp_adef};
 
+
+always @(posedge clk) begin
+    if (reset) begin
+        wait_data_ok <= 1'b0;
+    end
+    else if (fs_valid && !fs_ready_go && (flush_sign || br_taken && !br_stall)) begin
+        wait_data_ok <= 1'b1;
+    end
+    else if (inst_data_ok && wait_data_ok) begin
+        wait_data_ok <= 1'b0;
+    end
+end
+
 // IF stage
-assign fs_ready_go    = 1'b1;
+assign fs_ready_go    = !wait_data_ok && inst_data_ok || fs_inst_valid || fs_excp;
 assign fs_allowin     = !fs_valid || fs_ready_go && ds_allowin || flush_sign;
 assign fs_to_ds_valid =  fs_valid && fs_ready_go;
 always @(posedge clk) begin
@@ -75,7 +136,7 @@ always @(posedge clk) begin
     else if (fs_allowin) begin
         fs_valid <= to_fs_valid;
     end
-    else if (br_taken_cancel || flush_sign) begin
+    else if (flush_sign || br_taken && !br_stall) begin
         fs_valid <= 1'b0;
     end
 
@@ -89,13 +150,22 @@ always @(posedge clk) begin
         fs_excp      <= pfs_excp;
         fs_excp_num  <= pfs_excp_num;
     end
-end
 
-assign inst_sram_en    = to_fs_valid && fs_allowin;
-assign inst_sram_wen   = 4'h0;
-assign inst_sram_addr  = nextpc;
-assign inst_sram_wdata = 32'b0;
+    if (reset) begin
+        fs_inst_valid <= 1'b0;
+    end
+    else if (flush_sign || br_taken && !br_stall) begin
+        fs_inst_valid <= 1'b0;
+    end
+    else if (ds_allowin) begin
+        fs_inst_valid <= 1'b0;
+    end
+    else if (inst_data_ok && !ds_allowin) begin
+        fs_inst_valid <= 1'b1;
+        fs_inst_r <= inst_rdata;
+    end
+end
 
-assign fs_inst         = inst_sram_rdata;
+assign fs_inst = fs_inst_valid ? fs_inst_r : inst_rdata;
 
 endmodule
diff --git a/mem_stage.v b/mem_stage.v
index 3b38835..842a6e1 100644
--- a/mem_stage.v
+++ b/mem_stage.v
@@ -24,7 +24,8 @@ module mem_stage(
     input  wire            refetch_flush ,
     output wire            excp_out      ,
     //from data sram
-    input  wire [31:0] data_sram_rdata
+    input  wire        data_data_ok,
+    input  wire [31:0] data_rdata  
 );
 
 reg         ms_valid;
@@ -35,6 +36,7 @@ wire flush_sign = excp_flush || ertn_flush || refetch_flush;
 reg [`ES_TO_MS_BUS_WD -1:0] es_to_ms_bus_r;
 wire [ 3:0] ms_mul_div_op;
 wire        ms_res_from_mem;
+wire        ms_mem_we;
 wire        ms_gr_we;
 wire [ 4:0] ms_dest;
 wire [31:0] ms_alu_result;
@@ -48,16 +50,17 @@ wire        ms_excp;
 wire        ms_csr_we;
 wire [13:0] ms_csr_waddr;
 wire [31:0] ms_csr_wdata;
-assign {ms_excp_num    ,  //164:159
-        ms_ertn        ,  //158:158
-        ms_excp        ,  //157:157
-        ms_csr_we      ,  //156:156
-        ms_csr_waddr   ,  //155:142
-        ms_csr_wdata   ,  //141:110
-        ms_mem_size    ,  //109:108
-        ms_mem_sign_ext,  //107:107
-        ms_mem_addr    ,  //106:75
-        ms_mul_div_op  ,  //74:71
+assign {ms_excp_num    ,  //165:160
+        ms_ertn        ,  //159:159
+        ms_excp        ,  //158:158
+        ms_csr_we      ,  //157:157
+        ms_csr_waddr   ,  //156:143
+        ms_csr_wdata   ,  //142:111
+        ms_mem_size    ,  //110:109
+        ms_mem_sign_ext,  //108:108
+        ms_mem_addr    ,  //107:76
+        ms_mul_div_op  ,  //75:72
+        ms_mem_we      ,  //71:71
         ms_res_from_mem,  //70:70
         ms_gr_we       ,  //69:69
         ms_dest        ,  //68:64
@@ -65,6 +68,8 @@ assign {ms_excp_num    ,  //164:159
         ms_pc             //31:0
        } = es_to_ms_bus_r;
 
+reg         mem_reg_valid;
+reg  [31:0] mem_result_r;
 wire [31:0] mem_result;
 wire [31:0] ms_final_result;
 wire        excp;
@@ -83,7 +88,8 @@ assign ms_to_ws_bus = {ms_mem_addr    ,  //156:125
                        ms_pc             //31:0
                       };
 
-assign ms_ready_go    = 1'b1;
+assign ms_ready_go    =  (ms_res_from_mem || ms_mem_we) && (data_data_ok || mem_reg_valid) || 
+                        !(ms_res_from_mem || ms_mem_we) || excp;
 assign ms_allowin     = !ms_valid || ms_ready_go && ws_allowin;
 assign ms_to_ws_valid = ms_valid && ms_ready_go;
 always @(posedge clk) begin
@@ -100,31 +106,46 @@ always @(posedge clk) begin
     if (es_to_ms_valid && ms_allowin) begin
         es_to_ms_bus_r  <= es_to_ms_bus;
     end
+
+    if (reset) begin
+        mem_reg_valid <= 1'b0;
+    end
+    else if (flush_sign) begin
+        mem_reg_valid <= 1'b0;
+    end
+    else if (ws_allowin) begin
+        mem_reg_valid <= 1'b0;
+    end
+    else if (data_data_ok && !ws_allowin) begin
+        mem_reg_valid <= 1'b1;
+        mem_result_r <= mem_result;
+    end
 end
 
-wire [7:0] mem_byte = ({8{ms_mem_addr[1:0]==2'b00}} & data_sram_rdata[ 7: 0]) |
-                      ({8{ms_mem_addr[1:0]==2'b01}} & data_sram_rdata[15: 8]) |
-                      ({8{ms_mem_addr[1:0]==2'b10}} & data_sram_rdata[23:16]) |
-                      ({8{ms_mem_addr[1:0]==2'b11}} & data_sram_rdata[31:24]) ; 
+wire [7:0] mem_byte = ({8{ms_mem_addr[1:0]==2'b00}} & data_rdata[ 7: 0]) |
+                      ({8{ms_mem_addr[1:0]==2'b01}} & data_rdata[15: 8]) |
+                      ({8{ms_mem_addr[1:0]==2'b10}} & data_rdata[23:16]) |
+                      ({8{ms_mem_addr[1:0]==2'b11}} & data_rdata[31:24]) ; 
                                                             
 
-wire [15:0] mem_half = ({16{ms_mem_addr[1:0]==2'b00}} & data_sram_rdata[15: 0]) |
-                       ({16{ms_mem_addr[1:0]==2'b10}} & data_sram_rdata[31:16]) ;
+wire [15:0] mem_half = ({16{ms_mem_addr[1:0]==2'b00}} & data_rdata[15: 0]) |
+                       ({16{ms_mem_addr[1:0]==2'b10}} & data_rdata[31:16]) ;
 
 assign mem_result = ({32{ms_mem_size[0] &&  ms_mem_sign_ext}} & {{24{mem_byte[ 7]}}, mem_byte}) |
                     ({32{ms_mem_size[0] && ~ms_mem_sign_ext}} & { 24'b0            , mem_byte}) |
                     ({32{ms_mem_size[1] &&  ms_mem_sign_ext}} & {{16{mem_half[15]}}, mem_half}) |
                     ({32{ms_mem_size[1] && ~ms_mem_sign_ext}} & { 16'b0            , mem_half}) |
-                    ({32{!ms_mem_size}}                       & data_sram_rdata               ) ;
+                    ({32{!ms_mem_size}}                       & data_rdata               ) ;
 
-assign ms_final_result = ({32{ms_res_from_mem }}                  & mem_result       )  |
-                         ({32{ms_mul_div_op[0]}}                  & mul_result[31: 0])  |
-                         ({32{ms_mul_div_op[1]}}                  & mul_result[63:32])  |
-                         ({32{ms_mul_div_op[2]}}                  & div_result       )  |
-                         ({32{ms_mul_div_op[3]}}                  & mod_result       )  |
-                         ({32{!ms_mul_div_op & !ms_res_from_mem}} & ms_alu_result);
+assign ms_final_result = ({32{ms_res_from_mem }} & (mem_reg_valid ? mem_result_r : mem_result)      )  |
+                         ({32{ms_mul_div_op[0]}}                                 & mul_result[31: 0])  |
+                         ({32{ms_mul_div_op[1]}}                                 & mul_result[63:32])  |
+                         ({32{ms_mul_div_op[2]}}                                 & div_result       )  |
+                         ({32{ms_mul_div_op[3]}}                                 & mod_result       )  |
+                         ({32{!ms_mul_div_op & !ms_res_from_mem}}                & ms_alu_result);
 
-assign ms_to_ds_forward_bus = {ms_valid && ms_gr_we, ms_dest, ms_final_result};
+wire dep_need_stall = ms_valid && ms_gr_we && !ms_ready_go;
+assign ms_to_ds_forward_bus = {dep_need_stall, ms_to_ws_valid && ms_gr_we, ms_dest, ms_final_result};
 
 // excp
 assign excp     = ms_excp & ms_valid;
diff --git a/mycpu.h b/mycpu.h
index 2f9ddd2..850c00f 100644
--- a/mycpu.h
+++ b/mycpu.h
@@ -4,10 +4,10 @@
     `define BR_BUS_WD       34
     `define FS_TO_DS_BUS_WD 66
     `define DS_TO_ES_BUS_WD 216
-    `define ES_TO_MS_BUS_WD 165
+    `define ES_TO_MS_BUS_WD 166
     `define MS_TO_WS_BUS_WD 157
     `define WS_TO_RF_BUS_WD 38
     `define ES_TO_DS_FORWARD_BUS 39
-    `define MS_TO_DS_FORWARD_BUS 38
+    `define MS_TO_DS_FORWARD_BUS 39
     `define WS_TO_DS_FORWARD_BUS 38
 `endif
diff --git a/mycpu_top.v b/mycpu_top.v
index 9256bb0..3d46f0e 100644
--- a/mycpu_top.v
+++ b/mycpu_top.v
@@ -4,16 +4,24 @@ module mycpu_top(
     input  wire        clk,
     input  wire        resetn,
     // inst sram interface
-    output wire        inst_sram_en,
-    output wire [ 3:0] inst_sram_we,
+    output wire        inst_sram_req,
+    output wire        inst_sram_wr,
+    output wire [ 1:0] inst_sram_size,
+    output wire [ 3:0] inst_sram_wstrb,
     output wire [31:0] inst_sram_addr,
     output wire [31:0] inst_sram_wdata,
+    input  wire        inst_sram_addr_ok,
+    input  wire        inst_sram_data_ok,
     input  wire [31:0] inst_sram_rdata,
     // data sram interface
-    output wire        data_sram_en,
-    output wire [ 3:0] data_sram_we,
+    output wire        data_sram_req,
+    output wire        data_sram_wr,
+    output wire [ 1:0] data_sram_size,
+    output wire [ 3:0] data_sram_wstrb,
     output wire [31:0] data_sram_addr,
     output wire [31:0] data_sram_wdata,
+    input  wire        data_sram_addr_ok,
+    input  wire        data_sram_data_ok,
     input  wire [31:0] data_sram_rdata,
     // trace debug interface
     output wire [31:0] debug_wb_pc,
@@ -94,11 +102,15 @@ if_stage if_stage(
     .csr_eentry     (fs_csr_eentry  ),
     .csr_era        (fs_csr_era     ),
     //inst sram interface
-    .inst_sram_en   (inst_sram_en   ),
-    .inst_sram_wen  (inst_sram_wen  ),
-    .inst_sram_addr (inst_sram_addr ),
-    .inst_sram_wdata(inst_sram_wdata),
-    .inst_sram_rdata(inst_sram_rdata)
+    .inst_req       (inst_sram_req    ),
+    .inst_wr        (inst_sram_wr     ),
+    .inst_size      (inst_sram_size   ),
+    .inst_addr      (inst_sram_addr   ),
+    .inst_wstrb     (inst_sram_wstrb  ),
+    .inst_wdata     (inst_sram_wdata  ),
+    .inst_addr_ok   (inst_sram_addr_ok),
+    .inst_data_ok   (inst_sram_data_ok),
+    .inst_rdata     (inst_sram_rdata  )
 );
 // ID stage
 id_stage id_stage(
@@ -158,10 +170,13 @@ exe_stage exe_stage(
     .refetch_flush  (refetch_flush  ),
     .ms_excp        (ms_excp        ),
     //data sram interface
-    .data_sram_en   (data_sram_en   ),
-    .data_sram_we   (data_sram_we   ),
-    .data_sram_addr (data_sram_addr ),
-    .data_sram_wdata(data_sram_wdata)
+    .data_req       (data_sram_req    ),
+    .data_wr        (data_sram_wr     ),
+    .data_size      (data_sram_size   ),
+    .data_addr      (data_sram_addr   ),
+    .data_wstrb     (data_sram_wstrb  ),
+    .data_wdata     (data_sram_wdata  ),
+    .data_addr_ok   (data_sram_addr_ok)
 );
 
 div u_div(
@@ -210,7 +225,8 @@ mem_stage mem_stage(
     .refetch_flush  (refetch_flush  ),
     .excp_out       (ms_excp        ),
     //from data sram
-    .data_sram_rdata(data_sram_rdata)
+    .data_data_ok   (data_sram_data_ok),
+    .data_rdata     (data_sram_rdata  )
 );
 // WB stage
 wb_stage wb_stage(
-- 
2.34.1

