本实验新增了部分CSR寄存器和5种例外：
- 在CSR模块增加了计时器、BadV、中断的输入输出端口
- 在IF级增加ADEF例外，在ID级增加中断、BREAK和INE例外，在MEM级增加ALE例外
- 在ID级实现读取计时器的逻辑，在WB级增加输出BadV的逻辑

注意事项：
- 计时器的读取没有放在执行级，而是放在了译码级
- 手册上未定义IS[11]在复位时需要初始化，但是为了避免trace比对出现X，还是进行了初始化
