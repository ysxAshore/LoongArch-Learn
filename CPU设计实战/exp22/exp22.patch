From 40d1810d08641a257548e50a0c9960a487b3bd87 Mon Sep 17 00:00:00 2001
From: liangliang678 <wujunliang@loongson.cn>
Date: Fri, 7 Jul 2023 10:27:32 +0800
Subject: [PATCH] exp22

---
 axi_bridge.v | 123 ++++++++++++++++++++-----------
 cache.v      |  75 ++++++++++++++-----
 exe_stage.v  |  21 ++++--
 if_stage.v   |  17 +++--
 mycpu_top.v  | 202 +++++++++++++++++++++++++++++++++++++++------------
 5 files changed, 319 insertions(+), 119 deletions(-)

diff --git a/axi_bridge.v b/axi_bridge.v
index 47b8592..ebf9c0d 100644
--- a/axi_bridge.v
+++ b/axi_bridge.v
@@ -43,25 +43,33 @@ module axi_bridge(
     input  wire         bvalid,
     output reg          bready,
 
-    input  wire           inst_req     ,
-    input  wire           inst_wr      ,
-    input  wire [ 1:0]    inst_size    ,
-    input  wire [31:0]    inst_addr    ,
-    input  wire [ 3:0]    inst_wstrb   ,
-    input  wire [31:0]    inst_wdata   ,
-    output wire           inst_addr_ok ,
-    output wire           inst_data_ok ,
-    output wire [31:0]    inst_rdata   ,
-
-    input  wire           data_req     ,
-    input  wire           data_wr      ,
-    input  wire [ 1:0]    data_size    ,
-    input  wire [31:0]    data_addr    ,
-    input  wire [ 3:0]    data_wstrb   ,
-    input  wire [31:0]    data_wdata   ,
-    output wire           data_addr_ok ,
-    output wire           data_data_ok ,
-    output wire [31:0]    data_rdata   
+    input  wire           inst_rd_req     ,
+    input  wire [ 2:0]    inst_rd_type    ,
+    input  wire [31:0]    inst_rd_addr    ,
+    output wire           inst_rd_rdy     ,
+    output wire           inst_ret_valid  ,
+    output wire           inst_ret_last   ,
+    output wire [31:0]    inst_ret_data   ,
+    input  wire           inst_wr_req     ,
+    input  wire [ 2:0]    inst_wr_type    ,
+    input  wire [31:0]    inst_wr_addr    ,
+    input  wire [ 3:0]    inst_wr_wstrb   ,
+    input  wire [127:0]   inst_wr_data    ,
+    output wire           inst_wr_rdy     ,
+
+    input  wire           data_rd_req     ,
+    input  wire [ 2:0]    data_rd_type    ,
+    input  wire [31:0]    data_rd_addr    ,
+    output wire           data_rd_rdy     ,
+    output wire           data_ret_valid  ,
+    output wire           data_ret_last   ,
+    output wire [31:0]    data_ret_data   ,
+    input  wire           data_wr_req     ,
+    input  wire [ 2:0]    data_wr_type    ,
+    input  wire [31:0]    data_wr_addr    ,
+    input  wire [ 3:0]    data_wr_wstrb   ,
+    input  wire [127:0]   data_wr_data    ,
+    output wire           data_wr_rdy     
 );
 
 //fixed signal
@@ -90,14 +98,22 @@ localparam write_wait_b = 3'b110;
 reg       read_requst_state;
 reg [2:0] write_requst_state;
 
-assign data_addr_ok = !data_wr && (read_requst_state == read_requst_empty) ||
-                       data_wr && (write_requst_state == write_request_empty);
-assign inst_addr_ok = !inst_wr && !(data_req & !data_wr) && (read_requst_state == read_requst_empty);
+reg [127:0] write_buffer_data;
+reg [  2:0] write_buffer_num;
+wire        write_buffer_last;
+assign write_buffer_last = write_buffer_num == 3'b1;
 
-assign inst_data_ok = !rid[0] && rvalid;
-assign inst_rdata  = rdata;
-assign data_data_ok =  rid[0] && rvalid || bvalid;
-assign data_rdata  = rdata;
+assign data_rd_rdy = (read_requst_state == read_requst_empty);
+assign data_wr_rdy = (write_requst_state == write_request_empty);
+assign inst_rd_rdy = !data_rd_req && (read_requst_state == read_requst_empty);
+assign inst_wr_rdy = 1'b1;
+
+assign inst_ret_valid = !rid[0] && rvalid;
+assign inst_ret_data  = rdata;
+assign inst_ret_last  = rlast;
+assign data_ret_valid = rid[0] && rvalid;
+assign data_ret_data  = rdata;
+assign data_ret_last  = rlast;
 
 always @(posedge clk) begin
     if (reset) begin
@@ -106,20 +122,20 @@ always @(posedge clk) begin
     end
     else case (read_requst_state)
         read_requst_empty: begin
-            if (data_req && !data_wr) begin
+            if (data_rd_req) begin
                 read_requst_state <= read_requst_ready;
                 arid <= 4'b1;
-                araddr <= data_addr;
-                arsize <= data_size;
-                arlen  <= 8'b0;
+                araddr <= data_rd_addr;
+                arsize <= (data_rd_type == 3'b100) ? 3'b10 : data_rd_type;
+                arlen  <= (data_rd_type == 3'b100) ? 8'b11 : 8'b0;
                 arvalid <= 1'b1;
             end
-            else if (inst_req & !inst_wr) begin
+            else if (inst_rd_req) begin
                 read_requst_state <= read_requst_ready;
                 arid <= 4'b0;
-                araddr <= inst_addr;
-                arsize <= inst_size;
-                arlen  <= 8'b0;
+                araddr <= inst_rd_addr;
+                arsize <= (inst_rd_type == 3'b100) ? 3'b10 : inst_rd_type;
+                arlen  <= (inst_rd_type == 3'b100) ? 8'b11 : 8'b0;
                 arvalid <= 1'b1;
             end
         end
@@ -145,18 +161,29 @@ always @(posedge clk) begin
         wvalid  <= 1'b0;
         wlast   <= 1'b0;
         bready  <= 1'b0;
+
+        write_buffer_num   <= 3'b0;
+        write_buffer_data  <= 128'b0;
     end
     else case (write_requst_state)
         write_request_empty: begin
-            if (data_req && data_wr) begin
+            if (data_wr_req) begin
                 write_requst_state <= write_data_wait;
-                awaddr  <= data_addr;
-                awsize  <= data_size;
-                awlen   <= 8'b0;
+                awaddr  <= data_wr_addr;
+                awsize  <= (data_wr_type == 3'b100) ? 3'b10 : data_wr_type;
+                awlen   <= (data_wr_type == 3'b100) ? 8'b11 : 8'b0;
                 awvalid <= 1'b1;
-                wdata   <= data_wdata;
-                wstrb   <= data_wstrb;
-                wlast <= 1'b1;
+                wdata   <= data_wr_data[31:0];
+                wstrb   <= data_wr_wstrb;
+
+                write_buffer_data <= {32'b0, data_wr_data[127:32]};
+                if (data_wr_type == 3'b100) begin
+                    write_buffer_num <= 3'b011;
+                end
+                else begin
+                    write_buffer_num <= 3'b0;
+                    wlast <= 1'b1;
+                end
             end
         end
         write_data_wait: begin
@@ -167,12 +194,24 @@ always @(posedge clk) begin
             end
         end 
         write_data_transform: begin
-            if (wready) begin
+            if (wready && wlast) begin
                 write_requst_state <= write_wait_b;
                 wvalid <= 1'b0;
                 wlast <= 1'b0;
                 bready <= 1'b1;
             end
+            else if (wready) begin
+                if (write_buffer_last) begin
+                    wlast <= 1'b1;
+                end
+            
+                write_requst_state <= write_data_transform;
+
+                wdata   <= write_buffer_data[31:0];
+                wvalid  <= 1'b1;
+                write_buffer_data <= {32'b0, write_buffer_data[127:32]};
+                write_buffer_num  <= write_buffer_num - 3'b1;
+            end
         end
 	    write_wait_b: begin
             if (bvalid && bready) begin
diff --git a/cache.v b/cache.v
index 8a44bd8..9106c8e 100644
--- a/cache.v
+++ b/cache.v
@@ -1,10 +1,11 @@
 module cache
 (
     input  wire              clk          ,
-    input  wire              resetn       ,
+    input  wire              reset        ,
     //to from cpu
     input  wire              valid        ,
     input  wire              op           ,
+    input  wire [ 2:0]       size         ,
     input  wire [ 7:0]       index        ,
     input  wire [19:0]       tag          ,
     input  wire [ 3:0]       offset       ,
@@ -13,6 +14,7 @@ module cache
     output wire              addr_ok      ,
     output wire              data_ok      ,
     output wire [31:0]       rdata        ,
+    input  wire              uncache_en   ,
     //to from axi
     output wire              rd_req       ,
     output wire [ 2:0]       rd_type      ,
@@ -33,11 +35,13 @@ reg [255:0] way0_d_reg;
 reg [255:0] way1_d_reg;
 
 reg         request_buffer_op         ;
+reg [ 2:0]  request_buffer_size       ;
 reg [ 7:0]  request_buffer_index      ;
 reg [19:0]  request_buffer_tag        ;
 reg [ 3:0]  request_buffer_offset     ;
 reg [ 3:0]  request_buffer_wstrb      ;
 reg [31:0]  request_buffer_wdata      ;
+reg         request_buffer_uncache_en ;
 
 reg          miss_buffer_replace_way ;
 reg  [ 1:0]  miss_buffer_ret_num     ;
@@ -150,6 +154,10 @@ wire         main_state_is_refill ;
 wire         write_state_is_idle;
 wire         write_state_is_full;
 
+wire         uncache_wr     ;
+wire [ 2:0]  uncache_wr_type;
+reg          rd_req_buffer  ;
+
 wire         way0_wr_en;
 wire         way1_wr_en;
 
@@ -170,15 +178,17 @@ reg       write_buffer_state;
 //state machine
 //main loop
 always @(posedge clk) begin
-    if (!resetn) begin
+    if (reset) begin
         main_state <= main_idle;
 
         request_buffer_op         <=  1'b0;
+        request_buffer_size       <=  3'b0;
         request_buffer_index      <=  8'b0;
         request_buffer_tag        <= 20'b0;
         request_buffer_offset     <=  4'b0;
         request_buffer_wstrb      <=  4'b0;
         request_buffer_wdata      <= 32'b0;
+        request_buffer_uncache_en <=  1'b0;
 
         miss_buffer_replace_way <= 1'b0;
 
@@ -190,11 +200,14 @@ always @(posedge clk) begin
                 main_state <= main_lookup;
 
                 request_buffer_op         <= op        ;
+                request_buffer_size       <= size      ;
                 request_buffer_index      <= index     ;
                 request_buffer_tag        <= tag       ;
                 request_buffer_offset     <= offset    ;
                 request_buffer_wstrb      <= wstrb     ;
                 request_buffer_wdata      <= wdata     ;
+
+                request_buffer_uncache_en <= uncache_en;
             end
         end
         main_lookup: begin
@@ -202,11 +215,14 @@ always @(posedge clk) begin
                 main_state <= main_lookup;
 
                 request_buffer_op         <= op        ;
+                request_buffer_size       <= size      ;
                 request_buffer_index      <= index     ;
                 request_buffer_offset     <= offset    ;
                 request_buffer_tag        <= tag       ;
                 request_buffer_wstrb      <= wstrb     ;
                 request_buffer_wdata      <= wdata     ;
+
+                request_buffer_uncache_en <= uncache_en;
             end
             else if (!cache_hit) begin
                 main_state <= main_miss;
@@ -221,7 +237,9 @@ always @(posedge clk) begin
 
                 miss_buffer_replace_way <= replace_way;
 
-                wr_req <= replace_d && replace_v;
+                wr_req <= uncache_wr || 
+                          ((replace_d && replace_v) && !request_buffer_uncache_en);
+                          
             end
         end
         main_replace: begin
@@ -235,7 +253,7 @@ always @(posedge clk) begin
             end
         end
         main_refill: begin
-            if ((ret_valid && ret_last)) begin
+            if ((ret_valid && ret_last) || !rd_req_buffer) begin   //when rd_req is not set, go to next state directly
                 main_state <= main_idle;
             end
             else begin
@@ -252,7 +270,7 @@ end
 
 //hit write state 
 always @(posedge clk) begin
-    if (!resetn) begin
+    if (reset) begin
         write_buffer_state  <= write_buffer_idle;
 
         write_buffer_index  <= 8'b0;
@@ -306,7 +324,8 @@ assign addr_ok = (main_state_is_idle && main_idle2lookup) || (main_state_is_look
 //tag compare
 assign way0_hit  = way0_tagv_douta[0] && (request_buffer_tag == way0_tagv_douta[20:1]);   //this signal will not maintain
 assign way1_hit  = way1_tagv_douta[0] && (request_buffer_tag == way1_tagv_douta[20:1]);
-assign cache_hit = (way0_hit || way1_hit);
+assign cache_hit = (way0_hit || way1_hit) && !(request_buffer_uncache_en);  //uncache road reuse
+//when cache inst op mode2 no hit, main state machine will still go a round. implement easy.
 
 //data select
 assign way0_data = {way0_bank3_douta, way0_bank2_douta, way0_bank1_douta, way0_bank0_douta};
@@ -318,7 +337,7 @@ assign load_res  = {32{way0_hit}} & way0_load_word |
 
 //data_ok logic
 assign data_ok = ((main_state_is_lookup && (cache_hit || request_buffer_op)) || 
-                  (main_state_is_refill && !request_buffer_op && ret_valid && (miss_buffer_ret_num == request_buffer_offset[3:2])));
+                  (main_state_is_refill && !request_buffer_op && ret_valid && ((miss_buffer_ret_num == request_buffer_offset[3:2]) || request_buffer_uncache_en)));
 
 /*====================================main state miss=======================================*/
 
@@ -335,17 +354,21 @@ assign replace_v    = replace_way ? way1_tagv_douta[0] : way0_tagv_douta[0];
 assign replace_tag  = miss_buffer_replace_way ? way1_tagv_douta[20:1] : way0_tagv_douta[20:1];
 assign replace_data = miss_buffer_replace_way ? way1_data : way0_data;
 
-assign wr_type  = 3'b100;     //replace cache line
-assign wr_addr  = {replace_tag, request_buffer_index, 4'b0};
-assign wr_data  = replace_data;
-assign wr_wstrb = 4'hf;
+assign uncache_wr_type = request_buffer_size;
+assign uncache_wr = request_buffer_uncache_en && request_buffer_op;
 
-assign rd_req  = main_state_is_replace;
+assign wr_type  = uncache_wr ? uncache_wr_type : 3'b100;     //replace cache line
+assign wr_addr  = uncache_wr ? {request_buffer_tag, request_buffer_index, request_buffer_offset} :
+                               {replace_tag, request_buffer_index, 4'b0};
+assign wr_data  = uncache_wr ? {96'b0, request_buffer_wdata} : replace_data;
+assign wr_wstrb = uncache_wr ? request_buffer_wstrb : 4'hf;
+
+assign rd_req  = main_state_is_replace && !(uncache_wr);
 
 /*===================================main state refill======================================*/
 
-assign rd_type = 3'b100;
-assign rd_addr = {request_buffer_tag, request_buffer_index, 4'b0};
+assign rd_type = request_buffer_uncache_en ? request_buffer_size : 3'b100;
+assign rd_addr = request_buffer_uncache_en ? {request_buffer_tag, request_buffer_index, request_buffer_offset} : {request_buffer_tag, request_buffer_index, 4'b0};
 
 //rdate connect with ret_data dirctly. maintain one clock only
 
@@ -359,11 +382,23 @@ assign refill_data = (request_buffer_op && (request_buffer_offset[3:2] == miss_b
 assign way0_wr_en = !miss_buffer_replace_way && ret_valid;  //when rd_req is not set, ret_valid and ret_last will not be set. block will not be wr also.
 assign way1_wr_en =  miss_buffer_replace_way && ret_valid;
 
+always @(posedge clk) begin
+    if (reset) begin
+        rd_req_buffer <= 1'b0;
+    end
+    else if (rd_req) begin
+        rd_req_buffer <= 1'b1;
+    end
+    else if (main_state_is_refill && (ret_valid && ret_last)) begin
+        rd_req_buffer <= 1'b0;
+    end
+end
+
 /*==========================================================================================*/
 
 //refill or write state update dirty reg
 always @(posedge clk) begin
-    if (main_state_is_refill && ((ret_valid && ret_last))) begin
+    if (main_state_is_refill && ((ret_valid && ret_last) || !rd_req_buffer) && (!(request_buffer_uncache_en))) begin
         if (miss_buffer_replace_way) begin  //clear dirty flags
             way1_d_reg[request_buffer_index] <= request_buffer_op;
         end
@@ -451,7 +486,7 @@ assign way1_bank3_dina = bank_dina;
 
 /*===============================bank ena logic=================================*/
 
-assign bank_ena = 1'b1;
+assign bank_ena = !(request_buffer_uncache_en) || main_state_is_idle || main_state_is_lookup;
 
 assign way0_bank0_ena = bank_ena;
 assign way0_bank1_ena = bank_ena;
@@ -472,7 +507,7 @@ assign way1_tagv_addra = tagv_addra;
 
 /*===============================tagv ena logic=================================*/
 
-assign tagv_ena = 1'b1;
+assign tagv_ena = (!request_buffer_uncache_en) || main_state_is_idle || main_state_is_lookup;
 
 assign way0_tagv_ena  = tagv_ena;
 assign way1_tagv_ena  = tagv_ena;
@@ -586,7 +621,7 @@ tagv_sram way1_tagv(
 
 lfsr lfsr(
     .clk        (clk        )   ,
-    .resetn     (resetn     )   ,
+    .reset      (reset      )   ,
     .random_val (chosen_way )
 );
 
@@ -604,7 +639,7 @@ endmodule
 module lfsr
 ( 
     input  wire         clk         ,
-    input  wire         resetn      ,
+    input  wire         reset       ,
 
     output wire         random_val  
 );
@@ -612,7 +647,7 @@ module lfsr
 reg [7:0] r_lfsr;
 
 always @(posedge clk) begin
-    if (!resetn) begin
+    if (reset) begin
         r_lfsr <= 8'b1;
     end
     else begin
diff --git a/exe_stage.v b/exe_stage.v
index 143fb77..927a395 100644
--- a/exe_stage.v
+++ b/exe_stage.v
@@ -49,11 +49,14 @@ module exe_stage(
     // data sram interface
     output wire        data_req    ,
     output wire        data_wr     ,
-    output wire [ 1:0] data_size   ,
-    output wire [31:0] data_addr   ,
+    output wire [ 2:0] data_size   ,
+    output wire [ 7:0] data_index  ,
+    output wire [19:0] data_tag    ,
+    output wire [ 3:0] data_offset ,
     output wire [ 3:0] data_wstrb  ,
     output wire [31:0] data_wdata  ,
-    input  wire        data_addr_ok
+    input  wire        data_addr_ok,
+    output wire        data_uncache
 );
 
 reg         es_valid      ;
@@ -241,13 +244,17 @@ wire [31:0] es_sth_data = {{16{es_sth_wen[3]}} & es_rkd_value[15:0] ,
 
 assign data_req    = (es_res_from_mem || es_mem_we) && es_valid && ms_allowin && !flush_sign && !ms_excp && !excp;
 assign data_wr     = es_mem_we;
-assign {data_wstrb, data_size}  = ({6{es_mem_size[0]}} & {es_stb_wen, 2'b00}) |
-                                  ({6{es_mem_size[1]}} & {es_sth_wen, 2'b01}) |
-                                  ({6{!es_mem_size  }} & {4'b1111   , 2'b10}) ;
-assign data_addr  = data_paddr;
+assign {data_wstrb, data_size}  = ({7{es_mem_size[0]}} & {es_stb_wen, 3'b00}) |
+                                  ({7{es_mem_size[1]}} & {es_sth_wen, 3'b01}) |
+                                  ({7{!es_mem_size  }} & {4'b1111   , 3'b10}) ;
+assign {data_tag, data_index, data_offset}  = data_paddr;
 assign data_wdata = ({32{es_mem_size[0]}} & es_stb_data ) |
                     ({32{es_mem_size[1]}} & es_sth_data ) |
                     ({32{!es_mem_size  }} & es_rkd_value) ;
+assign data_uncache= (da_mode && (csr_datm == 2'b0))                 ||
+                     (dmw0_en && (csr_dmw0[`DMW_MAT] == 2'b0))       ||
+                     (dmw1_en && (csr_dmw1[`DMW_MAT] == 2'b0))       ||
+                     (data_addr_trans_en && (data_tlb_mat == 2'b0));
 
 // tlb
 assign es_invtlb_asid = es_rj_value[9:0];
diff --git a/if_stage.v b/if_stage.v
index 7563a9b..6ffeb73 100644
--- a/if_stage.v
+++ b/if_stage.v
@@ -41,13 +41,16 @@ module if_stage(
     // inst sram interface
     output wire        inst_req    ,
     output wire        inst_wr     ,
-    output wire [ 1:0] inst_size   ,
-    output wire [31:0] inst_addr   ,
+    output wire [ 2:0] inst_size   ,
+    output wire [ 7:0] inst_index  ,
+    output wire [19:0] inst_tag    ,
+    output wire [ 3:0] inst_offset ,
     output wire [ 3:0] inst_wstrb  ,
     output wire [31:0] inst_wdata  ,
     input  wire        inst_addr_ok,
     input  wire        inst_data_ok,
-    input  wire [31:0] inst_rdata  
+    input  wire [31:0] inst_rdata  ,
+    output wire        inst_uncache
 );
 
 reg         fs_valid;
@@ -134,10 +137,14 @@ reg wait_data_ok;
 
 assign inst_req    = fs_allowin && !wait_data_ok && !br_stall && !pfs_excp;
 assign inst_wr     = 1'h0;
-assign inst_size   = 2'h2;
-assign inst_addr   = inst_paddr;
+assign inst_size   = 3'h2;
+assign {inst_tag, inst_index, inst_offset} = inst_paddr;
 assign inst_wstrb  = 4'b0;
 assign inst_wdata  = 32'b0;
+assign inst_uncache= (!csr_pg && csr_da && csr_datf == 2'b0)         ||
+                     (dmw0_en && (csr_dmw0[`DMW_MAT] == 2'b0))       ||
+                     (dmw1_en && (csr_dmw1[`DMW_MAT] == 2'b0))       ||
+                     (inst_addr_trans_en && (inst_tlb_mat == 2'b0));
 
 wire pfs_excp_adef = (nextpc[0] || nextpc[1]);
 wire pfs_excp_tlbr = !inst_tlb_found && inst_addr_trans_en;
diff --git a/mycpu_top.v b/mycpu_top.v
index e76e1c1..700b273 100644
--- a/mycpu_top.v
+++ b/mycpu_top.v
@@ -55,25 +55,59 @@ reg         reset;
 always @(posedge aclk) reset <= ~aresetn;
 
 // inst sram interface
-wire        inst_req;
-wire        inst_wr;
-wire [ 1:0] inst_size;
-wire [ 3:0] inst_wstrb;
-wire [31:0] inst_addr;
-wire [31:0] inst_wdata;
-wire        inst_addr_ok;
-wire        inst_data_ok;
-wire [31:0] inst_rdata;
+wire         inst_valid;
+wire         inst_op;
+wire [ 2:0]  inst_size;
+wire [ 7:0]  inst_index;
+wire [19:0]  inst_tag;
+wire [ 3:0]  inst_offset;
+wire [ 3:0]  inst_wstrb;
+wire [31:0]  inst_wdata;
+wire         inst_addr_ok;  
+wire         inst_data_ok;  
+wire [31:0]  inst_rdata;    
+wire         inst_uncache;
 // data sram interface
-wire        data_req;
-wire        data_wr;
-wire [ 1:0] data_size;
-wire [ 3:0] data_wstrb;
-wire [31:0] data_addr;
-wire [31:0] data_wdata;
-wire        data_addr_ok;
-wire        data_data_ok;
-wire [31:0] data_rdata;
+wire         data_valid;
+wire         data_op;
+wire [ 2:0]  data_size;
+wire [ 7:0]  data_index;
+wire [19:0]  data_tag;
+wire [ 3:0]  data_offset;
+wire [ 3:0]  data_wstrb;
+wire [31:0]  data_wdata;
+wire         data_addr_ok;  
+wire         data_data_ok;  
+wire [31:0]  data_rdata; 
+wire         data_uncache;
+   
+wire         inst_rd_req;   
+wire [ 2:0]  inst_rd_type;  
+wire [31:0]  inst_rd_addr;  
+wire         inst_rd_rdy;   
+wire         inst_ret_valid;
+wire         inst_ret_last; 
+wire [31:0]  inst_ret_data; 
+wire         inst_wr_req;   
+wire [ 2:0]  inst_wr_type;  
+wire [31:0]  inst_wr_addr;  
+wire [ 3:0]  inst_wr_wstrb; 
+wire [127:0] inst_wr_data;  
+wire         inst_wr_rdy;  
+   
+wire         data_rd_req;   
+wire [ 2:0]  data_rd_type;  
+wire [31:0]  data_rd_addr;  
+wire         data_rd_rdy;   
+wire         data_ret_valid;
+wire         data_ret_last; 
+wire [31:0]  data_ret_data; 
+wire         data_wr_req;   
+wire [ 2:0]  data_wr_type;  
+wire [31:0]  data_wr_addr;  
+wire [ 3:0]  data_wr_wstrb; 
+wire [127:0] data_wr_data;  
+wire         data_wr_rdy; 
 
 wire         ds_allowin;
 wire         es_allowin;
@@ -222,15 +256,18 @@ if_stage if_stage(
     .inst_tlb_mat      (inst_tlb_mat    ),
     .inst_tlb_plv      (inst_tlb_plv    ),
     //inst sram interface
-    .inst_req       (inst_req    ),
-    .inst_wr        (inst_wr     ),
+    .inst_req       (inst_valid  ),
+    .inst_wr        (inst_op     ),
     .inst_size      (inst_size   ),
-    .inst_addr      (inst_addr   ),
+    .inst_tag       (inst_tag    ),
+    .inst_index     (inst_index  ),
+    .inst_offset    (inst_offset ),
     .inst_wstrb     (inst_wstrb  ),
     .inst_wdata     (inst_wdata  ),
     .inst_addr_ok   (inst_addr_ok),
     .inst_data_ok   (inst_data_ok),
-    .inst_rdata     (inst_rdata  )
+    .inst_rdata     (inst_rdata  ),
+    .inst_uncache   (inst_uncache)
 );
 // ID stage
 id_stage id_stage(
@@ -311,13 +348,16 @@ exe_stage exe_stage(
     .data_tlb_mat   (data_tlb_mat   ),
     .data_tlb_plv   (data_tlb_plv   ),
     //data sram interface
-    .data_req       (data_req    ),
-    .data_wr        (data_wr     ),
+    .data_req       (data_valid  ),
+    .data_wr        (data_op     ),
     .data_size      (data_size   ),
-    .data_addr      (data_addr   ),
+    .data_tag       (data_tag    ),
+    .data_index     (data_index  ),
+    .data_offset    (data_offset ),
     .data_wstrb     (data_wstrb  ),
     .data_wdata     (data_wdata  ),
-    .data_addr_ok   (data_addr_ok)
+    .data_addr_ok   (data_addr_ok),
+    .data_uncache   (data_uncache)
 );
 
 div u_div(
@@ -566,25 +606,97 @@ axi_bridge u_axi_brige(
     .bvalid (bvalid ),
     .bready (bready ),
 
-    .inst_req     (inst_req     ),
-    .inst_wr      (inst_wr      ),
-    .inst_size    (inst_size    ),
-    .inst_addr    (inst_addr    ),
-    .inst_wstrb   (inst_wstrb   ),
-    .inst_wdata   (inst_wdata   ),
-    .inst_addr_ok (inst_addr_ok ),
-    .inst_data_ok (inst_data_ok ),
-    .inst_rdata   (inst_rdata   ),
-
-    .data_req     (data_req     ),
-    .data_wr      (data_wr      ),
-    .data_size    (data_size    ),
-    .data_addr    (data_addr    ),
-    .data_wstrb   (data_wstrb   ),
-    .data_wdata   (data_wdata   ),
-    .data_addr_ok (data_addr_ok ),
-    .data_data_ok (data_data_ok ),
-    .data_rdata   (data_rdata   )
+    .inst_rd_req    (inst_rd_req    ),  
+    .inst_rd_type   (inst_rd_type   ), 
+    .inst_rd_addr   (inst_rd_addr   ),
+    .inst_rd_rdy    (inst_rd_rdy    ),
+    .inst_ret_valid (inst_ret_valid ),
+    .inst_ret_last  (inst_ret_last  ),
+    .inst_ret_data  (inst_ret_data  ),
+    .inst_wr_req    (inst_wr_req    ),
+    .inst_wr_type   (inst_wr_type   ),
+    .inst_wr_addr   (inst_wr_addr   ),
+    .inst_wr_wstrb  (inst_wr_wstrb  ),
+    .inst_wr_data   (inst_wr_data   ),
+    .inst_wr_rdy    (inst_wr_rdy    ),
+
+    .data_rd_req    (data_rd_req    ),  
+    .data_rd_type   (data_rd_type   ), 
+    .data_rd_addr   (data_rd_addr   ),
+    .data_rd_rdy    (data_rd_rdy    ),
+    .data_ret_valid (data_ret_valid ),
+    .data_ret_last  (data_ret_last  ),
+    .data_ret_data  (data_ret_data  ),
+    .data_wr_req    (data_wr_req    ),
+    .data_wr_type   (data_wr_type   ),
+    .data_wr_addr   (data_wr_addr   ),
+    .data_wr_wstrb  (data_wr_wstrb  ),
+    .data_wr_data   (data_wr_data   ),
+    .data_wr_rdy    (data_wr_rdy    )
+);
+
+cache icache( 
+    .clk            (aclk           ),
+    .reset          (reset          ),
+    //to from cpu
+    .valid          (inst_valid     ),
+    .op             (inst_op        ),   
+    .size           (inst_size      ),  
+    .index          (inst_index     ),
+    .tag            (inst_tag       ),
+    .offset         (inst_offset    ),
+    .wstrb          (inst_wstrb     ),
+    .wdata          (inst_wdata     ),
+    .addr_ok        (inst_addr_ok   ),
+    .data_ok        (inst_data_ok   ),
+    .rdata          (inst_rdata     ),
+    .uncache_en     (inst_uncache   ),
+    //to from axi 
+    .rd_req         (inst_rd_req    ), 
+    .rd_type        (inst_rd_type   ), 
+    .rd_addr        (inst_rd_addr   ), 
+    .rd_rdy         (inst_rd_rdy    ), 
+    .ret_valid      (inst_ret_valid ),    
+    .ret_last       (inst_ret_last  ), 
+    .ret_data       (inst_ret_data  ), 
+    .wr_req         (inst_wr_req    ), 
+    .wr_type        (inst_wr_type   ), 
+    .wr_addr        (inst_wr_addr   ), 
+    .wr_wstrb       (inst_wr_wstrb  ), 
+    .wr_data        (inst_wr_data   ), 
+    .wr_rdy         (inst_wr_rdy    )
+);
+
+cache dcache( 
+    .clk            (aclk           ),
+    .reset          (reset          ),
+    //to from cpu
+    .valid          (data_valid     ),
+    .op             (data_op        ),
+    .size           (data_size      ),
+    .index          (data_index     ),
+    .tag            (data_tag       ),
+    .offset         (data_offset    ),
+    .wstrb          (data_wstrb     ),
+    .wdata          (data_wdata     ),
+    .addr_ok        (data_addr_ok   ),
+    .data_ok        (data_data_ok   ),
+    .rdata          (data_rdata     ),
+    .uncache_en     (data_uncache   ),
+    //to from axi 
+    .rd_req         (data_rd_req    ), 
+    .rd_type        (data_rd_type   ), 
+    .rd_addr        (data_rd_addr   ), 
+    .rd_rdy         (data_rd_rdy    ), 
+    .ret_valid      (data_ret_valid ),    
+    .ret_last       (data_ret_last  ), 
+    .ret_data       (data_ret_data  ), 
+    .wr_req         (data_wr_req    ), 
+    .wr_type        (data_wr_type   ), 
+    .wr_addr        (data_wr_addr   ), 
+    .wr_wstrb       (data_wr_wstrb  ), 
+    .wr_data        (data_wr_data   ), 
+    .wr_rdy         (data_wr_rdy    )
 );
 
 endmodule
-- 
2.34.1

